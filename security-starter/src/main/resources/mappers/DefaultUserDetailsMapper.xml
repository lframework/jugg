<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lframework.starter.security.mappers.DefaultUserDetailsMapper">

    <resultMap id="result" type="com.lframework.starter.web.components.security.DefaultUserDetails">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="available" column="available"/>
    </resultMap>

    <select id="findByUsername" resultType="com.lframework.starter.web.components.security.DefaultUserDetails">
        SELECT id, username, password, name, available FROM sys_user
        WHERE username = #{username}
    </select>
</mapper>
